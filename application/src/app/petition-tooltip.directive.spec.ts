import { PetitionTooltipDirective } from './petition-tooltip.directive';

describe('PetitionTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new PetitionTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
